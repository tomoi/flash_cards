import type { FlashCardProps } from '../interfaces'

export default function EditFlashCard({
    subjectData,
    updateSubjectData,
    cardIndex,
    setCardIndex,
}: FlashCardProps) {
    return <p>Edit Flash Card</p>
}
